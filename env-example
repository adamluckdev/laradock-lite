# Path to website files on you machine
APP_CODE_PATH_HOST=../www

# Path to website files in container
APP_CODE_PATH_CONTAINER=/var/www

# Used for container data such as DB files
DATA_PATH_HOST=./data

PHP_VERSION=8.1

PHP_FPM_PUID=1000
PHP_FPM_PGID=1000

NGINX_HOST_LOG_PATH=./logs/nginx
NGINX_HOST_SITES_PATH=./nginx/sites
NGINX_HOST_HTTP_PORT=80
NGINX_PHP_UPSTREAM_CONTAINER=php-fpm
NGINX_PHP_UPSTREAM_PORT=9000

MARIADB_VERSION=latest
MARIADB_DATABASE=default
MARIADB_USER=default
MARIADB_PASSWORD=secret
MARIADB_PORT=3306
MARIADB_ROOT_PASSWORD=root
MARIADB_ENTRYPOINT_INITDB=./mariadb/docker-entrypoint-initdb.d
MARIADB_TIME_ZONE=Europe/Bratislava

POSTGRES_VERSION=alpine
POSTGRES_DB=default
POSTGRES_USER=default
POSTGRES_PASSWORD=secret
POSTGRES_PORT=5432
POSTGRES_TIME_ZONE=UTC

ADMINER_HOST_HTTP_PORT=8080

REDIS_PORT=6379

REDIS_WEBUI_USERNAME=laradock
REDIS_WEBUI_PASSWORD=laradock
REDIS_WEBUI_CONNECT_HOST=redis
REDIS_WEBUI_CONNECT_PORT=6379
REDIS_WEBUI_PORT=9987
